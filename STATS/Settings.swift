import SwiftUI

struct Settings: View {
    var body: some View {
        Text("Settings")
            .font(.largeTitle)
        //TODO: Need to add a List with setting options sections
    }
}

#Preview {
    Settings()
}
